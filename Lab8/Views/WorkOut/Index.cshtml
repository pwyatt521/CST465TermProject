@model IEnumerable<WorkOutModel>
@{
    ViewData["Title"] = "WorkOut";
}
<body>
    <h1>
        WorkOuts
    </h1>
    <h2>
    <div>
        <a asp-area="" asp-controller="WorkOut" asp-action="PostBlog">Post Blog</a>
    </div>
    </h2>

<ul class="list-group">
    @foreach (var WorkOut in Model)
    {
        <li class="list-group-item">
            <div class="row">
                <div class="col-md-10">
                    <p>
                        <a href="@Url.Action("View", "WorkOut", new { @id = WorkOut.ID })">@WorkOut.WName</a>
                    </p>
                </div>
                <div class="col-md-2">
                    <form class ="form-inline" asp-action="Delete" asp-controller="WorkOut" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="id" value="@WorkOut.ID" />
                        <button type="submit" class="btn btn-danger"><i class="fas fa-times"></i></button>
                    </form>
                </div>
            </div>
        </li>

    }
</ul>
</body>